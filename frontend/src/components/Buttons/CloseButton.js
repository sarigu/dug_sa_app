import React from 'react';
import CloseIcon from '../../icons/CloseIcon'
import './Buttons.css'

const CloseButton = () => {
    return (
        <div >
            <CloseIcon />
        </div>
    );
};

export default CloseButton;